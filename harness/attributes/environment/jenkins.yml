
attributes:
  app:
    build: static
    mode: production
    version: =exec("git log -n 1 --pretty=format:'%H'")
  magento:
    config:
      session:
        save: redis
        redis:
          hostname: redis
          database: 0
      cache:
        save: redis
        redis:
          hostname: redis
          database: 1
      fpc:
        save: redis
        redis:
          hostname: redis
          database: 2
  php:
    fpm:
      hostname: php-fpm
  database:
    host: mysql
