#!/bin/bash
set -e

function task_npm_install()
{
    (
        cd {{ @('frontend.path') }}
        
        if [ ! -f "package.json" ]; then
            return 0;
        fi

        npm install $@
    )
}
