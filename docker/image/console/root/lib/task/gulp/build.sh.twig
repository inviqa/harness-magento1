#!/bin/bash
set -e

function task_gulp_build()
{
    (
        cd {{ @('frontend.path') }}
        
        if [ ! -f "package.json" ]; then
            return 0;
        fi

        node_modules/.bin/gulp build $@
    )
}
